/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export', 
  basePath: '/portafolio-esteban', 
  assetPrefix: '/portafolio-esteban/', 
};

export default nextConfig;
